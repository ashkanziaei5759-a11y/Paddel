/**
 * تصویر (یا تصاویر) پنل سمت راست صفحه‌ی ورود.
 *
 * ── افزودن عکس خودتان ─────────────────────────────────────────────────────
 * فایل را با نام `poster.jpg` در مسیر `public/images/login/` کپی کنید.
 * همین و تمام — صفحه‌ی ورود خودکار از آن استفاده می‌کند.
 *
 * اگر بیش از یک تصویر در فهرست باشد، اسلایدر به‌صورت خودکار می‌چرخد و نقطه‌های
 * ناوبری نمایش داده می‌شوند. با یک تصویر، ثابت می‌ماند.
 *
 * `hasOwnArtwork: true` یعنی تصویر خودش تایپوگرافی دارد؛ در این حالت متن
 * فارسی روی تصویر نوشته نمی‌شود تا دو لایه‌ی نوشته روی هم نیفتد.
 * ──────────────────────────────────────────────────────────────────────────
 */

export const LOGIN_SLIDES = [
  { src: '/images/login/poster.jpg', alt: 'پوستر باشگاه پدل پرشین پدل' },
] as const;

/** تصویر پوستری است (متن روی آن نوشته نشود) */
export const HAS_OWN_ARTWORK = true;

/** اگر فایل بالا موجود نباشد، این تصویر موقت نمایش داده می‌شود */
export const FALLBACK_SLIDE = '/images/login/court-1.jpg';
