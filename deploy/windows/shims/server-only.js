/**
 * جایگزین خالیِ بسته‌ی `server-only`.
 *
 * آن بسته یک نگهبان زمان‌ساخت برای Next است: اگر ماژولی که فقط باید روی سرور
 * اجرا شود به باندل مرورگر راه پیدا کند، عمداً خطا می‌دهد. اسکریپت seed اصلاً
 * مرورگر ندارد — یک برنامه‌ی خط فرمان روی سرور است — ولی چون همان ماژول‌های
 * اپ را وارد می‌کند، آن نگهبان سر راهش قرار می‌گیرد و اجرا را متوقف می‌کند.
 *
 * پس هنگام باندل‌کردن seed، این فایل جای آن می‌نشیند.
 */
module.exports = {};
